# FinScope UK Frontend

Next.js dashboard for the FinScope UK API.

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not running on `http://localhost:8000`.
